---
title: 'Servers'
description: 'Servers that will act as send or receive data'
---

## Setup your server

Setup server and deploy on all the location where you want to send or receive to start using it.

<img
    className="block dark:hidden"
    src="/images/representation-light.svg"
    alt="Hero Light"
/>
<img
    className="hidden dark:block"
    src="/images/representation-dark.svg"
    alt="Hero Dark"
/>

We have created an `install.sh` which will automatically install mongodb as daemon and run zetarya server as daemon.

<Note>
    Run `daemon.xx.xx.bin` for the first time manually to setup the license server ip and the name of the location
</Note>

